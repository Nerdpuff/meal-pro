import { TotalDirective } from './total.directive';

describe('TotalDirective', () => {
  it('should create an instance', () => {
    const directive = new TotalDirective();
    expect(directive).toBeTruthy();
  });
});
